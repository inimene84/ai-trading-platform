# GPU / training-node extras. Do not install on the VPS Decision Engine.
# The engine re-reads metrics.json; it never recomputes DSR/PBO.
purgedcv>=0.1.6
purgedcv[optuna]>=0.1.6
pyarrow>=25.0.1
joblib>=1.3.0
